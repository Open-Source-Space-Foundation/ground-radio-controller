source [find interface/cmsis-dap.cfg]
source [find target/rp2350.cfg]

if {![info exists ELF]} {
    error "ELF not set. Pass it on the command line, e.g. -c \"set ELF /path/to/app.elf\""
}

adapter serial $env(OPENOCD_ADAPTER_SERIAL)

adapter speed 5000

program $ELF verify reset exit
