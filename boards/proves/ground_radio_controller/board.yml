# SPDX-License-Identifier: Apache-2.0
#
# Ground Radio Controller — DTS uses upstream Pico 2 base + ground_radio_hardware.dtsi only.

board:
  name: ground_radio_controller
  full_name: Ground Radio Controller
  vendor: PROVES
  socs:
    - name: rp2350a
